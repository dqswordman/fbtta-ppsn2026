seed: 0

dataset:
  name: cifar100
  num_classes: 100

paths:
  data_root: data
  output_root: artifacts/source_model_cifar100

training:
  epochs: 50
  batch_size: 512
  eval_batch_size: 512
  val_size: 5000
  lr: 0.1
  momentum: 0.9
  weight_decay: 0.0005
  num_workers: 4
  amp: true
